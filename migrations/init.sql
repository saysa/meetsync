-- Init script for PostgreSQL
-- The database and user are created automatically via POSTGRES_DB / POSTGRES_USER env vars.
-- Add any additional setup (extensions, schemas) here if needed.

SELECT 1;
